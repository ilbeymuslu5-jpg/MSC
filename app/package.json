{
  "name": "msc-music-app",
  "version": "1.0.0",
  "private": true,
  "description": "MSC Music — müzik ekosistemi mobil uygulaması (Capacitor / Android)",
  "type": "module",
  "scripts": {
    "build:www": "node build-www.mjs",
    "icons": "node make-icons.mjs",
    "sync": "npm run build:www && npx cap sync android",
    "open:android": "npx cap open android",
    "assemble:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "bundle:release": "npm run sync && cd android && ./gradlew bundleRelease",
    "apk:release": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
